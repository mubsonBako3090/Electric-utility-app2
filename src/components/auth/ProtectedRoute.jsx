'use client';
import React from 'react';

export default function ProtectedRoute({ children }) {
  // Minimal placeholder — in App Router you may implement middleware or context checks
  return <>{children}</>;
}
